import { defineConfig } from 'storybook';
import storybookTheme from './storybookTheme';

export default defineConfig({
  // Otras configuraciones de Storybook

  // Agrega la configuración de tema
  theme: storybookTheme,
});